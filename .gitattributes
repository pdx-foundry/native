# Hash-pinned replay artifacts must retain exact bytes on every checkout host.
tests/fixtures/** -text
# Packaged Python and generated wire bindings have byte identities across hosts.
src/**/*.py text eol=lf
